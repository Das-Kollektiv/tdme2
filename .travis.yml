language: cpp
compiler: gcc
os: 
  - linux
  - osx
dist: bionic
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - make
      - g++-9
      - libglew-dev
      - libopenal-dev
      - libglfw3-dev

script:
  - make mains
