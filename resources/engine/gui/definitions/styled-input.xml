<template>	
	<element id="{$id}" width="{$width}" height="{$height}" vertical-align="center" border="1" border-color="{$color.element_frame}" disabled="{$disabled}" focusable="true" on-initialize="{$on-initialize}" on-mouse-over="{$on-mouse-over}" on-mouse-click="{$on-mouse-click}" on-mouse-doubleclick="{$on-mouse-doubleclick}" on-mouse-out="{$on-mouse-out}" on-change="{$on-change}" background-color="{$color.element_midground}" options="{$options}" show-on="{$show-on}" hide-on="{$hide-on}" tooltip="{$tooltip}">
		<scrollarea width="100%" height="100%" alignment="none" background-color="{$color.element_background}">
			<styled-text id="{$id}_inner" width="auto" height="auto" font="{$font}" size="{$size}" color="{$color}" editable="true" preformatted="{$preformatted}">
				<![CDATA[
					{__InnerXML__}
				]]>
			</styled-text>
		</scrollarea>
	</element>
</template>